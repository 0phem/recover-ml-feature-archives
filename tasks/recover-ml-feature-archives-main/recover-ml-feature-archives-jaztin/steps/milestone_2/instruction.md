Once the catalog has been created, extend `/app/recover.py` so it loads `catalog.json` into DuckDB and generates `/app/facts/opa_input.json` for the recovery policy. The output should contain the catalog entries in a `shards` array along with a `required_columns` list and a `min_rows` value of `500`. Each shard should also include a `has_required_columns` field that is true only when the columns `customer_id`, `tenure`, `monthly_charges`, and `label` are all present. This field must be computed directly in the DuckDB query rather than being added later in Python.
